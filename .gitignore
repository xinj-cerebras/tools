# Generated by install.sh (symlinks to each tool's entry points).
# Keep the leading slash: a bare "bin/" would also match <tool>/bin/, which is
# where the actual scripts live, and the repo would install nothing.
/bin/

__pycache__/
*.pyc
.DS_Store
